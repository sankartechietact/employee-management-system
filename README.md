# Employee Management System

A full-stack **Employee Management System** built using **Spring Boot**, **Thymeleaf**, and **MySQL/MariaDB**.  
This application allows organizations to manage employee records with complete CRUD operations.

## 🚀 Features
- Employee Create, Read, Update, Delete (CRUD)
- Spring Boot MVC architecture
- Thymeleaf-based UI
- Form validation
- Global Exception Handling
- Database integration using JPA & Hibernate

## 🛠 Tech Stack
- Java 17
- Spring Boot
- Spring Data JPA
- Thymeleaf
- MySQL / MariaDB
- Maven

## 📂 Project Structure
- `controller` – Handles HTTP requests
- `service` – Business logic
- `repository` – Database operations
- `entity` – JPA entities
- `templates` – Thymeleaf HTML pages

## ▶️ How to Run
1. Clone the repository  
2. Configure database in `application.properties`
3. Run the application using:
4. Open browser: http://localhost:8080
